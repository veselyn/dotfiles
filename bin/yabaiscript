#!/usr/bin/env sh

. "$(dirname "$0")/_yabaiscript/services.sh"
. "$(dirname "$0")/_yabaiscript/resize.sh"

case $1 in
start | stop | restart)
  services "$1" "$2"
  ;;
resize)
  resize "$2"
  ;;
esac
